<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                    xmlns:views="clr-namespace:ProductCatalogue.Views"
                    xmlns:viewmodels="clr-namespace:ProductCatalogue.ViewModels"
                    >
    <DataTemplate DataType="{x:Type viewmodels:OverViewViewModel }">
        <views:OverviewView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type viewmodels:CreateViewModel }">
        <views:CreateView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type viewmodels:EditViewModel }">
        <views:EditView />
    </DataTemplate>

    <DataTemplate DataType="{x:Type viewmodels:AddUserWindowViewModel }">
        <views:AddUserWindow />
    </DataTemplate>


</ResourceDictionary>